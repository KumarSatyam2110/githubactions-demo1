name: First pipeline
on:
  push: 
    branches: [ main ]
  workflow_dispatch: 

jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        
      - name: Print hello
        run: echo "hello from Linux VM"
